import { EmailLowerCaseDirective } from './email-lower-case.directive';

describe('EmailLowerCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailLowerCaseDirective();
    expect(directive).toBeTruthy();
  });
});
